const Discord = require("discord.js")

module.exports.run = async (bot, message, args) => {
   return  message.channel.send("*what are you doing step bro!*")
}

module.exports.help = {
    name: "stepsis"
}
